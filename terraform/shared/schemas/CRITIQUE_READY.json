{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "CRITIQUE.READY",
  "description": "The critic agent produced an adversarial review of the architect's plan. Advisory; does not gate the pipeline.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "event_id",
    "type",
    "timestamp",
    "run_id",
    "correlation_id",
    "actor_id",
    "payload"
  ],
  "properties": {
    "schema_version": { "type": "string", "enum": ["1.0"] },
    "event_id": { "type": "string", "format": "uuid" },
    "type": { "type": "string", "enum": ["CRITIQUE.READY"] },
    "timestamp": { "type": "string", "format": "date-time" },
    "run_id": { "type": "string", "format": "uuid" },
    "correlation_id": { "type": "string", "format": "uuid" },
    "causation_id": { "type": ["string", "null"], "format": "uuid" },
    "actor_id": { "type": "string", "minLength": 1, "maxLength": 128 },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["project_slug", "critique_s3_key", "issue_count", "summary", "session_id"],
      "properties": {
        "project_slug": { "type": "string", "minLength": 1, "maxLength": 64 },
        "critique_s3_key": { "type": "string", "minLength": 1 },
        "issue_count": { "type": "integer", "minimum": 0 },
        "high_severity_count": { "type": "integer", "minimum": 0 },
        "medium_severity_count": { "type": "integer", "minimum": 0 },
        "low_severity_count": { "type": "integer", "minimum": 0 },
        "summary": { "type": "string", "maxLength": 2048 },
        "session_id": { "type": "string", "minLength": 1 },
        "token_in": { "type": "integer", "minimum": 0 },
        "token_out": { "type": "integer", "minimum": 0 },
        "cost_usd": { "type": "number", "minimum": 0.0 },
        "duration_ms": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
